export const roomCreationPolicy = {
  enabled: false,
  maxDurationHours: 24,
  weeklyAllowance: null as number | null,
};

export const availabilityPolicy = { maxDurationHours: 24, gracePeriodHours: 2 };
export const roomArchivePolicy = { durationHours: 2 };
